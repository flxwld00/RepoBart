# Sales Performance Dashboard

A self-contained Streamlit sales dashboard with three in-code sample data sources:

- Orders
- Pipeline
- Customers

Run it with:

```powershell
pip install -r requirements.txt
streamlit run app.py
```
